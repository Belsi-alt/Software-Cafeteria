## 🗄️ Base de datos
El archivo SQL para la creación de la base de datos PostgreSQL
se encuentra en la carpeta `database`.

Para su uso:
1. Crear una base de datos en PostgreSQL
2. Ejecutar el script `database.sql`
